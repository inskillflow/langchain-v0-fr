services:
  app:
    build: .
    image: langchain-course-hello-world:latest
    container_name: langchain-hello-world
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/.venv
    extra_hosts:
      # Permet au conteneur d'atteindre Ollama tournant sur l'hote (cas Pratique 2 - Ollama).
      # Sur Windows et macOS, Docker Desktop fournit deja host.docker.internal automatiquement,
      # mais le declarer ici rend le compose portable y compris sur Linux.
      - "host.docker.internal:host-gateway"
    stdin_open: true
    tty: true
