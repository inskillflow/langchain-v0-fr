services:
  app:
    build: .
    image: langchain-hello-world-openai:latest
    container_name: langchain-hello-world-openai
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/.venv
    stdin_open: true
    tty: true
